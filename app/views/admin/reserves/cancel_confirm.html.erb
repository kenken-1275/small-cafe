<div class="reserve">
  <div class="reserve-center">
    <div class="reserve-title">管理　予約キャンセル確認画面</div>

    <div class="reserve-confirmation">下記ご予約をキャンセルしてよろしいでしょうか？</div>
      <div class="reserve-results">
        <div class="reserve-result">
          <div class="reserve-content">お名前：<%= @reservation.user.kanji_last_name %> <%= @reservation.user.kanji_first_name %>　様</div>
          <div class="reserve-content">日時：<%= @reservation.reservation_date %></div>
          <div class="reserve-content">時間：<%= @reservation.perse_reservation_time %></div>
          <div class="reserve-content">人数：<%= @reservation.people_number %></div>
          <div class="reserve-content">電話番号：<%= @reservation.tel_number %></div>
        </div>
      </div> 
      <div class="reserve-button">
        <%= link_to 'トップページに戻る',root_path,class:'reserve-submit'%>
        <%= link_to 'キャンセル確定',admin_reserf_path(@reservation),method: :delete,class:'reserve-submit'%>
      </div>
  </div>
</div>